# Noble | SEO

Next.js (App Router) site for [nobleseo.co](https://nobleseo.co).

```bash
npm install
npm run dev
```

Deploy the `noble-web` directory on Vercel. Set the project root to `noble-web` if this folder is not the repository root.
